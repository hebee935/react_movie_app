import React, { Component } from 'react';
import { Query } from 'react-apollo';
import { getOneMovie } from './queries';

class MovieDetailView extends Component {
    render() {
        return(
            <div></div>
        )
    }
}

export default MovieDetailView;